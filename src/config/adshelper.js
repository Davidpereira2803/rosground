import { AD_BANNER_UNIT_ID, AD_REWARDED_UNIT_ID } from "@env";

const ADS_CONFIG = {
    BANNER_UNIT_ID: AD_BANNER_UNIT_ID || "",
    REWARDED_UNIT_ID: AD_REWARDED_UNIT_ID || "",
};

export default ADS_CONFIG;